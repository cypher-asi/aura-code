[package]
name = "aura-agents"
version = "0.1.0"
edition = "2021"

[dependencies]
aura-core = { path = "../../infra/core" }
aura-store = { path = "../../infra/store" }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1"
